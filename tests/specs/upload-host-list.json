[
  {
    "name": "Upload host list shows access required for unapproved user",
    "steps": [
      {
        "send": { "callback": "upload:host_list" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "You need access to upload host lists. Request approval first."
            }
          }
        ]
      }
    ]
  }
]
