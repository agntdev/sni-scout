[
  {
    "name": "Upload host list",
    "steps": [
      {
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "Submit newline-separated host list for scanning"
            }
          }
        ],
        "send": {
          "callback": "upload:host_list"
        }
      }
    ]
  }
]
